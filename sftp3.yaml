- name: Transfer file from SFTP to Windows
  hosts: localhost
  gather_facts: no

  tasks:
    - name: Fetch file from SFTP server
      fetch:
        src: "sftp://kap-user@10.10.5.76/kap-user/test.txt"
        dest: "/path/on/awx/host/test.txt"
        flat: yes
        validate_certs: no
        fail_on_missing: yes
